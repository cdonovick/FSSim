#ifndef LSF_H
#define LSF_H

class LogFileSystem {
    public:
        /* 
         * capacity     - the capacity in bytes of the file system
         * segment_size - the size in bytes of a segment
         * block_size   - the size in bytes of a block
         *
         * clean        - ratio
         */
        LogFileSystem(unsigned fs_capacity, 
                unsigned segment_size, 
                unsigned block_size,


};
#endif

